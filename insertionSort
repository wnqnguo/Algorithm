function insertionSort(arr){
	for(var j =1; j <=arr.length;j++){
	   var i = j -1;
	   var value = arr[j];
	   while(i > 0){
	   	if(arr[i]<arr[i-1]){
	   		var temp = arr[i-1];
	   		arr[i-1] = arr[i];
	   		arr[i]= temp
	   	}
	   	i--;
	   }
	}
	return arr;
}
